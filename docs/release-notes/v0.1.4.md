# Clawdeck v0.1.4

This is a GitHub source release for the support route added after `v0.1.3`.

## Added

- `clawdeck support`: prints the optional $5 Codex run receipt link for npm users who want to pay after Clawdeck saves setup time.
- GitHub funding metadata and package funding metadata point to the same self-serve receipt path.

## Notes

- npm publish is still blocked by registry auth on this machine, so the npm package remains at `@nicdunz/clawdeck@0.1.2` until auth is restored.
- The GitHub repo contains the `0.1.4` source state and the support command.

## Support

Optional support receipt: https://nicdunz.gumroad.com/l/smrimu
